{
  "name": "etsy-tagger",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Background Playwright automation that tags Etsy orders and writes the 🏷️ private-note mirror. Replaces the 'Etsy Order Tags' Chrome extension.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "login": "node login.js",
    "sweep": "node run.js",
    "sweep:apply": "node run.js --apply",
    "dump-order": "node dump-order.js",
    "test-order": "node test-order.js"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
